import styled from 'styled-components';

const AccountStyled = styled.div`
  img {
    width: 40px;
    height: 40px;
    border-radius: 100%;

    &:hover {
      cursor: pointer;
    }
  }
`;

const AccountMenuWrapper = styled.div``;

export { AccountStyled, AccountMenuWrapper };
